var Sequelize = require('sequelize')

var productSchema = Product.build({
  // name: ,
  // color: String,
  // size: String,
  // createdAt: new Date(),
  // updatedAt: new Date(),
  // CategoryId: String,
  // MakerId: ,
});
